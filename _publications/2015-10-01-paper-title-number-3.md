title: "Legacy publications list (unused)"
collection: publications
published: false
permalink: /publications/legacy/
---

This Markdown file originally duplicated the entire publications list inside a single collection item. Each paper now lives in its own `_publications/*.md` file with richer metadata, so this legacy entry stays unpublished for archival reference only.
